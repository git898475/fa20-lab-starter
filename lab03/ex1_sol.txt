1. ①.data：声明数据段，存储初始化的全局变量/静态变量，为写入static区域（data segment）的部分。
    ②.word在内存中分配一个字（四字节）的空间，并初始化为一个32bits的整数值。n: .word 9的作用是在内存中分配1个字（4字节），存入数值 9，创建标签 n，指向这个内存地址。
    ③.text为写入代码区的部分，声明代码段，存储程序指令。
2. 34 为fibb(9)的值
3.如1中的②所述，n中的地址为0x10000010也即t3中的值，指向内存中的9。
4.将t3中的0x9改为0xD即可
